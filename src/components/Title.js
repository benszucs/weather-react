import React from 'react';
import PropTypes from 'prop-types';

const Title = props => {
  return(
    <div>
      <h1 className="text-center pt-4 title">Weather App</h1>
      <h3 className="text-center py-2 title">Using APIs in React</h3>
    </div>
  );
};

export default Title;
